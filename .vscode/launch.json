{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "bashdb",
            "request": "launch",
            "name": "setup",
            "cwd": "${workspaceFolder}",
            "program": "${workspaceFolder}/setup",
            "args": []
        }
    ]
}
